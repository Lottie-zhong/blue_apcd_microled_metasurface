# Per-source validity index

The authoritative per-source V2 validity and provenance records remain under `../sources/<case>/`. This index is a compact manifest; it does not duplicate large solver evidence.
