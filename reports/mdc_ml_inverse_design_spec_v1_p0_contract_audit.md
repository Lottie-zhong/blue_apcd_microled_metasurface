# MDC-ML P0-A contract audit with P0-B lineage clarification

P0-A established the authoritative baseline round-trip, 14-field inventory, tolerance semantics, APCD-ready proxy mathematics, executable Go/No-Go wording, reachable geometry bounds, material provenance, and external-array storage. Those results remain in force.

P0-B corrects two identity/activation semantics without changing the physical design space or thresholds:

- The P0-A `878c...` hash included material provenance. It is retained as a legacy material-bound physical hash, while the new canonical geometry v3 excludes source/raw-table hashes.
- The former five primary objectives are staged: four are active for nominal search; the fifth robustness objective activates only for shortlisted candidates with completed +/-3 nm labels.

No P0-A hash value is discarded. The frozen historical `c386...` value, the P0-A `878c...` value, and the new identity hierarchy are all recorded in the deterministic audit output.
