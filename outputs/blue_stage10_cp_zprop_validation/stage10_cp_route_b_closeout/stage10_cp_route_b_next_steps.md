# Stage10 CP Route-B Next Steps

## English

### Decision

Freeze `RB1_J1J2_D195_T90_PSI97p5_H525` as the current Stage10 CP robust dimer candidate. Pause additional finite-patch source-position expansion unless a later validation question specifically requires it.

### Priorities

1. **P0 - Preserve the frozen base.** Use D195_T90 as the CP dimer base for any later integration study. Do not alter its J1/J2 geometry casually.
2. **P1 - Validate integration only when needed.** A later DBR/RCLED or spacer study should start from this frozen candidate and must separately validate source normalization, collection definition, and absolute LEE.
3. **P2 - Keep a backup path.** Test `RB1_J1J2_D182p5_T85_PSI97p5_H525` at `x_plus_qp_x/y` only if D195_T90 needs an independent backup.
4. **P3 - Keep functions separated.** K=6 and steering remain outside this CP closeout and require their own validation branch.

### Stop Conditions

Do not launch a full x-y dipole-position sweep, DBR/RCLED study, K=6 layout, or steering study as part of this closeout. Cone-power values remain API-normalized extraction metrics, not independently validated absolute LEE.

## 中文

### 决策

冻结 `RB1_J1J2_D195_T90_PSI97p5_H525`，作为当前 Stage10 CP 鲁棒 dimer 候选。除非后续出现明确验证问题，否则暂停继续扩展有限阵列源位置扫描。

### 优先级

1. **P0 - 保持冻结基底。** 后续集成研究以 D195_T90 作为 CP dimer base，不应随意改变其 J1/J2 几何。
2. **P1 - 按需验证集成。** 后续 DBR/RCLED 或 spacer 研究应从该冻结候选出发，并单独验证源归一化、收集定义和绝对 LEE。
3. **P2 - 保留备选路径。** 仅在 D195_T90 需要独立备选时，测试 `RB1_J1J2_D182p5_T85_PSI97p5_H525` 的 `x_plus_qp_x/y`。
4. **P3 - 功能分支分离。** K=6 和 steering 不属于本 CP closeout，需要独立验证分支。

### 停止条件

本次 closeout 不启动完整 x-y 偶极位置扫描、DBR/RCLED、K=6 或 steering。cone power 仍标记为 API-normalized extraction metrics，不是独立验证的绝对 LEE。
