# K6 mesh attribution v1

Read-only M0/M1/M2 audit found no stored actual mesh-grid coordinates through the exposed post-FSP API. Mesh-accuracy increments alone cannot establish nesting. Conformal and material-order fields are partially unexposed, so classification is `ATTRIBUTION_INCONCLUSIVE`; a minimal mesh/index diagnostic setup-only contract is warranted, with no solver authorization in this task.
