# NP K6 / MDC interface pending decisions v1

Status: `PENDING_MDC_DECISION`

The MDC branch must explicitly decide coupling level, final integration stack, wavelength range, angular input, polarization/source basis, output tensor, common conventions, and formal-label compatibility before NP formal training can start. No decision is inferred automatically.

See `outputs/np_k6_pretraining_handoff_v1/mdc_np_interface_pending_decisions_v1.json` for the machine-readable checklist. `common_conventions` is `PENDING_CROSS_BRANCH_FREEZE`; all other decision groups are `PENDING_MDC_DECISION`.
